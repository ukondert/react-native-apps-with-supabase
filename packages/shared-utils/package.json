{
  "name": "@workout/shared-utils",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean",
    "lint": "tsc --noEmit",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}